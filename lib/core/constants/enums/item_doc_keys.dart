/// This enum is used to define the keys for Item documentations.
enum ItemDocKeys {
  id,
  itemName,

  image,
  fields,
  fieldID,

  /// Use this for fieldTypes
  fieldType,

  /// name of the field
  fieldName,

  /// value of the field
  fieldValue,
  createdDate,
}
